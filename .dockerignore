# Dependências instaladas no host (a app vive em src/, então o padrão precisa ser **/)
**/node_modules

# Controle de versão e metadados
.git
.gitignore
.claude

# Segredos e configuração local
.env
.env.*
!.env.example

# Os próprios artefatos Docker
Dockerfile
.dockerignore
docker-compose.yml
compose.yaml

# Logs e caches
**/npm-debug.log*
**/yarn-error.log*
**/.npm

# Editor / SO
.vscode
.idea
**/.DS_Store
*.Identifier

# Documentação e utilitários que não vão para a imagem
README.md
popula-dados.http
